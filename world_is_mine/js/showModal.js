if (showModal == 'radarChartModal') {
  radarChartModal.classList.remove('hidden');
} else if (showModal == 'userMsgModal') {
  userMsgModal.classList.remove('hidden');
} else if (showModal == 'rankingModal') {
  rankingModal.classList.remove('hidden');
} else if (showModal == 'sendMsgModal') {
  sendMsgModal.classList.remove('hidden');
} else if (showModal == 'loginModal') {
  loginModal.classList.remove('hidden');
} else if (showModal == 'signupModal') {
  signupModal.classList.remove('hidden');
} else if (showModal == 'cancelModal') {
  cancelModal.classList.remove('hidden');
}
